delay = 0;
myDelay = delay*textIndex;
t = (time - inPoint) - myDelay;
if (t >= 0) {
  freq =2.5;
  amplitude = 150;
  decay = 7.0;
  s = amplitude*Math.cos(freq*t*2*Math.PI)/Math.exp(decay*t);
  [s,s]
}
else {
  value
}
